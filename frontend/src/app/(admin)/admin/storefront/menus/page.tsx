"use client";

// Shared so the same menu builder renders here and inside the new admin shell.
import MenusManager from "@/components/admin/MenusManager";

export default function MenusPage() {
  return <MenusManager />;
}
