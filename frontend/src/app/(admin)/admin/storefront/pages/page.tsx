"use client";

// Shared so the same pages builder renders here and inside the new admin shell.
import PagesManager from "@/components/admin/PagesManager";

export default function PagesListPage() {
  return <PagesManager />;
}
