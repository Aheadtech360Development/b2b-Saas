"use client";

// Shared so the same product-template builder renders here and inside the new admin shell.
import ProductTemplatesManager from "@/components/admin/ProductTemplatesManager";

export default function ProductTemplatesPage() {
  return <ProductTemplatesManager />;
}
